package com.jsheets.expressions.types;

import com.jsheets.expressions.Expression;

/**
 * An expression that takes a value and returns
 * a {@code Number}.
 */
public interface NumericExpression<T> extends Expression<T, Number> {}
